# ===== 凭据（绝不上传） =====
credentials.md
# 凭据备份（.specbak_* 等派生名同样含 wx_bot_id/wx_bot_secret/api_key；精确名规则挡不住它们）
credentials.md*

# ===== Chrome 登录态 =====
chrome-profile/
# OKKI 任务独立 profile(与上一行同口径;2026-09-23 重建)
/chrome-profile-okki/

# ===== 运行日志（logs\） =====
logs/

# ===== 买家消息快照（data\，含对话 PII） =====
data/

# ===== 报告（reports\，含买家名/对话摘要） =====
reports/

# ===== 代码快照备份 =====
backups/

# ===== 运行时状态（scripts\） =====
scripts/state.json
scripts/state.json.bak
scripts/nudge_state.json
scripts/nudge_state.json.bak
scripts/summary_last.json
scripts/monitor.pid
scripts/watchdog.pid

# ===== 日志/临时/备份文件（任意位置） =====
*.log
*.bak
*.pre
*.pid
msgs_*
*_out.log
*_err.log

# ===== 测试夹具（虚构买家数据，需入库） =====
!tests/fixtures/
!tests/fixtures/msgs_*

# ===== 系统文件 =====
Thumbs.db
.DS_Store

# ===== Node 依赖（wecom_bot） =====
node_modules/

# ===== 报价提醒状态(含买家名 PII) =====
scripts/remind_state.json

# ===== 本地真实配置备份(不入库) =====
*.local

# ===== 同步时间戳(运行产物) =====
scripts/last_sync.json

# ===== 2026-09-07 本地发布补充 =====
# specs(含 PII 历史 REPORT)与本地工作流文档不入库
/specs/
/README_本地重建.md
# 根游离文件(来历不明,疑似误复制)
/package-lock.json
# 真实配置不入库(模板见 scripts\config.json.example)
/scripts/config.json
/tools/control-agent/config.json
/tools/wecom-connector/config.json
# 组件运行期
/tools/control-agent/data/
/tools/control-agent/logs/
/tools/wecom-connector/data/
/tools/wecom-connector/logs/
/tools/*/node_modules/
# 组件内部交付文档(含本机凭据配置说明,不入库)
/tools/control-agent/REPORT_交付报告.md
/tools/control-agent/REPORT_改造v0.4.md
# 遗留退役 bot 目录(已归档;保险排除)
/scripts/wecom/
# 重建备份区与 token
/_rebuild_backup_*/
/.git_token

# ===== 本地会话产物与归档（不入库） =====
/.opencode/
/alibaba/
/skills-main/

# ===== 2026-09-24 发布补充：脱敏审计新增排除 =====
# 代码/配置快照备份（任意层级；含本机真实路径与旧配置，属本地产物）
*.specbak_*
# 商业单据与客户案卷（PI/提单/报关/CBP 查验等真实单据，含客户名、地址、电话、
# 银行账号、箱号提单号，属客户机密，一律不入库）
/pi_export/
/pdf_read/
# 商业单据原件（PI 报价单/形式发票等，含客户与价格信息）
/PI原件*.xlsx
/PI原件*.pdf
# 扫描件图像（任意层级，防止单据图片被误加）
*.jpg
*.png
# 注意：tests/ 下的夹具为虚构数据，仍需入库
!tests/fixtures/*.png

# ===== 2026-09-26 清理补充 =====
# 与本项目无关的本机工具（C 盘缓存清理），2026-09-26 曾误入库，已移除并在此保险排除
/clean-c/
# 一次性验收工具（对应 spec 已归档；如需再跑可从 git 历史或仓库外的"已移除归档"目录取回）
/tools/status-verify/
# 仓库外的"已移除归档"目录若被误复制回部署根，排除之（归档目录名 Agent_work-removed_<时间戳>）
/Agent_work-removed_*/

